# Acceptance

The repair is acceptable only when the PR CI is green, the exact PR head is merged, and a new attempt-1 remote observation on the resulting exact main SHA satisfies all frozen quality, strict-zero, identity, privacy, authority, and worker shadow latency gates. The prior retained diagnostic Worker remains governed by its existing deletion reconciliation and is not modified by this PR.
