# Scope

In scope: transport-level partitioning of the frozen 24-query Qdrant batch into four concurrent six-query shards, deterministic result concatenation, compatibility preservation, and exact CI.

Out of scope: quality threshold changes, latency threshold changes, query reduction, dense-limit reduction, embedding changes, collection/index mutation, payload expansion, production serving, promotion, pointer changes, and blocker clearance.
