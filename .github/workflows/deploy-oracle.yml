name: M26 Daily IP Rate Limit Secret Restager

on:
  workflow_dispatch:
    inputs:
      confirmation:
        required: true
        type: string
      expected_image_id:
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: m26-daily-ip-rate-limit-production-restager
  cancel-in-progress: false

jobs:
  restage:
    if: inputs.confirmation == 'RESTAGE-M26-DAILY-IP-RATE-LIMIT'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    env:
      CURRENT_CONTAINER: m26-public-api-production-repair2-ownerhash-r2-auth-520aed
      EXPECTED_CURRENT_IMAGE: sha256:fe246e352f30af7fe8fdd02b756d7abd1cb6d6ca6e2fd922ff730ed1686273ec
      EXISTING_IMAGE_TAG: m26-daily-ip-rate-limit:33833281606-1
      EXPECTED_STAGED_IMAGE_ID: sha256:c5bcfbf63b43300c57ad92637c41bfc72dac8148dccabb2eea18f5751bfba8b1
      ORACLE_VM_HOST: ${{ vars.ORACLE_VM_HOST || secrets.ORACLE_VM_HOST }}
      ORACLE_VM_USER: ${{ vars.ORACLE_VM_USER || secrets.ORACLE_VM_USER }}

    steps:
      - name: Validate authority
        shell: bash
        run: |
          set -euo pipefail
          test "${{ inputs.confirmation }}" = "RESTAGE-M26-DAILY-IP-RATE-LIMIT"
          test "${{ inputs.expected_image_id }}" = "$EXPECTED_STAGED_IMAGE_ID"

      - name: Require secure inputs
        shell: bash
        env:
          SSH_PRIVATE_KEY: ${{ secrets.ORACLE_VM_SSH_PRIVATE_KEY }}
          OWNER_BYPASS_SHA: ${{ secrets.ASK_OWNER_BYPASS_TOKEN_SHA256 }}
          IP_HASH_SECRET: ${{ secrets.ASK_RATE_LIMIT_IP_HASH_SECRET }}
        run: |
          set -euo pipefail
          for name in SSH_PRIVATE_KEY ORACLE_VM_HOST ORACLE_VM_USER OWNER_BYPASS_SHA IP_HASH_SECRET; do
            test -n "${!name:-}" || { echo "missing required input: $name" >&2; exit 2; }
          done
          python3 - <<'PY'
          import os, re
          assert re.fullmatch(r"[0-9a-f]{64}", os.environ["OWNER_BYPASS_SHA"].strip().lower())
          assert len(os.environ["IP_HASH_SECRET"].strip()) >= 32
          print("PROMOTION_SECRET_SHAPE_GATE=PASS")
          PY

      - name: Configure SSH
        shell: bash
        env:
          SSH_PRIVATE_KEY: ${{ secrets.ORACLE_VM_SSH_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          install -m 700 -d ~/.ssh
          printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "$ORACLE_VM_HOST" >> ~/.ssh/known_hosts

      - name: Verify restored baseline and reusable image
        shell: bash
        run: |
          set -euo pipefail
          ssh "$ORACLE_VM_USER@$ORACLE_VM_HOST" \
            "CURRENT_CONTAINER='$CURRENT_CONTAINER' EXPECTED_CURRENT_IMAGE='$EXPECTED_CURRENT_IMAGE' EXISTING_IMAGE_TAG='$EXISTING_IMAGE_TAG' EXPECTED_STAGED_IMAGE_ID='$EXPECTED_STAGED_IMAGE_ID' bash -s" <<'REMOTE'
          set -euo pipefail
          test "$(docker inspect --format '{{.Image}}' "$CURRENT_CONTAINER")" = "$EXPECTED_CURRENT_IMAGE"
          test "$(docker image inspect --format '{{.Id}}' "$EXISTING_IMAGE_TAG")" = "$EXPECTED_STAGED_IMAGE_ID"
          curl --fail --silent --max-time 10 http://127.0.0.1:18087/v1/answers/health >/dev/null
          echo "RESTORED_BASELINE_AND_REUSABLE_IMAGE=PASS"
          REMOTE

      - name: Restage run-scoped env
        shell: bash
        env:
          OWNER_BYPASS_SHA: ${{ secrets.ASK_OWNER_BYPASS_TOKEN_SHA256 }}
          IP_HASH_SECRET: ${{ secrets.ASK_RATE_LIMIT_IP_HASH_SECRET }}
        run: |
          set -euo pipefail
          REMOTE_ENV="/tmp/m26-daily-ip-rate-limit-promotion-${{ github.run_id }}-${{ github.run_attempt }}.env"
          echo "REMOTE_ENV=$REMOTE_ENV" >> "$GITHUB_ENV"

          ssh "$ORACLE_VM_USER@$ORACLE_VM_HOST" \
            "umask 077; docker inspect --format '{{range .Config.Env}}{{println .}}{{end}}' '$CURRENT_CONTAINER' > '${REMOTE_ENV}.base'; chmod 600 '${REMOTE_ENV}.base'"

          fragment="$(mktemp)"
          chmod 600 "$fragment"
          {
            printf 'M26_ASK_OWNER_BYPASS_TOKEN_SHA256=%s\n' "$OWNER_BYPASS_SHA"
            printf 'M26_ASK_RATE_LIMIT_IP_HASH_SECRET=%s\n' "$IP_HASH_SECRET"
            printf 'M26_ASK_DAILY_IP_LIMIT=10\n'
            printf 'M26_ASK_RATE_LIMIT_DB_PATH=/var/lib/knowledge-engine/rate-limit/m26-daily-ip-rate-limit.sqlite3\n'
            printf 'M26_ASK_RATE_LIMIT_DAY_TZ=UTC\n'
          } > "$fragment"
          scp "$fragment" "$ORACLE_VM_USER@$ORACLE_VM_HOST:${REMOTE_ENV}.fragment"
          rm -f "$fragment"

          ssh "$ORACLE_VM_USER@$ORACLE_VM_HOST" \
            "REMOTE_ENV='$REMOTE_ENV' python3 - <<'PY'
          import os, re
          from pathlib import Path
          path = Path(os.environ['REMOTE_ENV'])
          base = Path(str(path) + '.base')
          fragment = Path(str(path) + '.fragment')
          drop = {
              'M26_ASK_OWNER_BYPASS_TOKEN_SHA256',
              'M26_ASK_RATE_LIMIT_IP_HASH_SECRET',
              'M26_ASK_DAILY_IP_LIMIT',
              'M26_ASK_RATE_LIMIT_DB_PATH',
              'M26_ASK_RATE_LIMIT_DAY_TZ',
          }
          out = []
          for line in base.read_text().splitlines():
              if '=' not in line:
                  continue
              key = line.split('=', 1)[0]
              if key not in drop:
                  out.append(line)
          out.extend(fragment.read_text().splitlines())
          path.write_text('\n'.join(out) + '\n')
          os.chmod(path, 0o600)
          values = dict(line.split('=', 1) for line in out if '=' in line)
          assert re.fullmatch(r'[0-9a-f]{64}', values['M26_ASK_OWNER_BYPASS_TOKEN_SHA256'])
          assert len(values['M26_ASK_RATE_LIMIT_IP_HASH_SECRET']) >= 32
          assert values['M26_ASK_DAILY_IP_LIMIT'] == '10'
          assert values['M26_ASK_RATE_LIMIT_DB_PATH'] == '/var/lib/knowledge-engine/rate-limit/m26-daily-ip-rate-limit.sqlite3'
          assert values['M26_ASK_RATE_LIMIT_DAY_TZ'] == 'UTC'
          base.unlink(missing_ok=True)
          fragment.unlink(missing_ok=True)
          print('RUN_SCOPED_ENV_RESTAGE=PASS')
          PY"

      - name: Non-secret receipt
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          cat > artifacts/restaging-receipt.json <<EOF
          {
            "reused_image_tag": "$EXISTING_IMAGE_TAG",
            "reused_image_id": "$EXPECTED_STAGED_IMAGE_ID",
            "remote_env_path": "$REMOTE_ENV",
            "production_container_mutated": false,
            "image_rebuilt": false,
            "secret_values_recorded": false
          }
          EOF

      - name: Upload receipt
        uses: actions/upload-artifact@v4
        with:
          name: m26-rate-limit-restaging-${{ github.run_id }}-${{ github.run_attempt }}
          path: artifacts/restaging-receipt.json
          retention-days: 7
