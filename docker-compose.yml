services:
  knowledge-engine:
    build: .
    image: ${M26_RUNTIME_IMAGE:-knowledge-engine:0.2.0}
    restart: unless-stopped
    env_file:
      - ${M26_RUNTIME_ENV_FILE:-.env}
    environment:
      M26_PUBLIC_QUOTA_DB: /var/lib/knowledge-engine/rate-limit/quota.sqlite3
      M26_QA_DB_PATH: /var/lib/knowledge-engine/public-api/qa-inbox.sqlite3
      M26_ADMIN_CONTROL_DB_PATH: /var/lib/knowledge-engine/public-api/admin-control.sqlite3
      M26_L3B_ADMIN_QUALIFIED: "true"
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - knowledge-engine-cache:/var/lib/knowledge-engine/cache
      - m26-daily-ip-rate-limit-data:/var/lib/knowledge-engine/rate-limit
      - m26-qa-inbox-data:/var/lib/knowledge-engine/public-api
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    command:
      - uvicorn
      - knowledge_engine.m26_console_api:app
      - --host
      - 0.0.0.0
      - --port
      - "8080"

volumes:
  knowledge-engine-cache:
  m26-daily-ip-rate-limit-data:
  m26-qa-inbox-data:
